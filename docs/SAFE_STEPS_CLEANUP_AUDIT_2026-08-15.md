# SafeSteps Cleanup Audit - 2026-08-15

This audit separates active app code from source packs, generated material, local tooling, and deletion candidates. It is intentionally conservative: no files were deleted in this pass.

## Current Rule

Do not decide from folder names alone. A folder is active if it is imported by `app/`, `components/`, `lib/`, referenced by `package.json`, used by `SafeStepsTools/`, or covered by tests.

## Active Runtime - Keep

These are part of the current Expo Router/Supabase app surface:

| Path | Why it stays |
| --- | --- |
| `app/` | Expo Router screens and route layouts. |
| `components/` | Shared UI used by app screens. |
| `lib/` | Product data, engines, contexts, Supabase access, and typed app logic. |
| `supabase/` | Active migrations, local Supabase config, functions, and tests. |
| `__tests__/` | Current verification coverage. |
| `assets/` | App assets. |
| `SafeStepsTools/` | Repo maintenance, import, audit, database, and smoke-test tooling. |
| `docs/` | Product, cleanup, audit, and technical documentation. |
| `curriculum/` | Current curriculum source used by repo tooling and product documentation. |
| `backend/` | Backend/server and security contracts remain present; inspect exact imports before moving. |
| `proof-of-change/` | Active: `lib/engines/learningAssessmentContentEngine.ts` imports `proof_of_change_tasks_001_100.json` directly. |

## Active Or Probably Active - Keep Until Focused Review

These are not safe deletion targets yet because the repo contains scripts, tests, or product docs that still acknowledge them:

| Path | Current evidence |
| --- | --- |
| `casefiles/` | Loose fixture files are represented by `lib/data/sampleCasefiles.ts`, but `SafeStepsTools/New-SafeStepsCasefile.ps1` still writes new casefiles here. Migrate that script before deleting. |
| `parents/` | Same status as `casefiles/`; generated by `SafeStepsTools/New-SafeStepsCasefile.ps1`. |
| `automation/` | `docs/FOUND_MATERIAL_RECONCILIATION.md` says `automation/create_casefile.ps1` delegates to `SafeStepsTools/New-SafeStepsCasefile.ps1`; keep until that workflow is retired. |
| `android/` | Native project folder; only remove if the app is intentionally Expo-managed only and native builds are regenerated. |
| `.github/` | CI/workflow surface. |
| `.vscode/` | Local MCP/editor config; inspect for secrets before sharing, but do not delete as app cleanup. |
| `.supabase/` | Supabase local state/config; do not delete while database tooling is in use. |
| `node_modules/` | Dependency install output; removable for disk cleanup only, not source cleanup. |

## Source Packs Or Archive Material - Archive Before Delete

These are likely not live app runtime, but may contain source material worth preserving or already-imported content:

| Path | Why it is not current runtime |
| --- | --- |
| `Challenges/` | Active challenges are in `lib/data/safestepsParentChallenges.ts`; only archived tooling references the root `Challenges` folder. |
| `challenges_clean/` | Only noted by the build map as a folder to treat carefully; no active import found in the audited app/lib/tool/test paths. |
| `snapshots/` | Old generated curriculum/app snapshots; not active runtime. Large enough to handle deliberately. |
| `stage1/` | Only archive/docs references found; active database stage-one material is under `supabase/tests` and `docs/database`. |
| `storybooks/` | Active storybook data is in `lib/data/interactiveStorybooks.ts`; root folder appears to be source/reference material. |
| `CHILD PROTECTION MANUAL FRAMEWORK GUIDELINES/` | Source material/reference, not current app runtime. Preserve or relocate under `docs/source-content/` if needed. |
| `sequence-knowledge/` | Source/reference material unless a focused import check proves otherwise. |
| `scenario_banks/` | Source/reference material unless a focused import check proves otherwise. |
| `data/` | Mixed source data. Audit subfolders before deleting because current typed data lives in `lib/data/`. |
| `resources/` | Mixed source/resource material. Current routed resource screens live in `app/resources/`. |
| `bulk/` | Source/import material unless a focused script dependency is found. |
| `forensic/` | Source/reference material unless a focused import check proves otherwise. |
| `schema/` and `database/` | Reference/schema material; active migrations are in `supabase/migrations/`. |
| `PowerShell/` | Older local module material. Reconciliation docs say external PowerShell modules should be mined selectively rather than copied wholesale. |

## Likely Cleanup Candidates

These look like generated output, old experiments, or local tool state. Confirm once more before deletion because the git index is currently not trustworthy.

| Path | Reason |
| --- | --- |
| `_generated_reports_20260629/` | Prior cleanup doc says one generated report file remained blocked by Windows permissions. |
| `caseworker-dashboard/` | Only archived legacy tooling references it; active dashboard is under `app/dashboard/`. |
| `frontend/` | Prior cleanup doc says old backend/frontend experimental copies were deleted in an earlier pass; current repo still has a root `frontend/` folder with no active reference found. |
| `export/` | Looks like generated output; no active reference found in audited paths. |
| `diff/` | Looks like generated comparison output; no active reference found in audited paths. |
| `ai/` | Local/source material candidate; no active reference found in audited paths. One filename suggests credential details, so treat as secret-review/quarantine material, not ordinary source. |
| `engines/` | Root folder is suspicious because active engines live in `lib/engines/`. |
| `styles/` | Root folder is suspicious because current styles are in `lib/styles.ts` and component styles. |
| `utils/` | Root folder is suspicious because active utilities should be under `lib/` or `SafeStepsTools/`; audit before delete. |
| `packages/` | Check for package workspace usage before deleting. No workspace config was found in `package.json`. |
| `cli/` | Likely standalone/legacy tooling unless still used manually. |

## Candidate Detail Pass

The first detail pass checked the likely cleanup candidates against active `package.json`, `app/`, `components/`, `lib/`, `SafeStepsTools/`, `__tests__/`, and `docs/` references.

| Path | Contents observed | Current classification |
| --- | --- | --- |
| `_generated_reports_20260629/` | `SafeSteps_FolderSearch.txt` only. | Delete candidate after git/index repair and permission check. |
| `caseworker-dashboard/` | Small legacy dashboard with `Dockerfile`, `src/App.js`, integration JS files, report pages. | Archive/delete candidate; only archived legacy tooling references it. |
| `frontend/` | Parallel React-style app/components/contexts/pages/services tree. | Archive/delete candidate; no active relative import found and current app equivalents live under `app/`, `components/`, and `lib/contexts`. |
| `export/` | `curriculum-bundle.json`. | Generated artifact; archive/delete candidate if source content exists elsewhere. |
| `diff/` | `curriculum-diff.json`. | Generated artifact; delete candidate if no current review needs it. |
| `ai/` | PowerShell helper scripts, prompt template, and a credential-looking note filename. | Quarantine/secret-review candidate. Do not publish or echo contents. |
| `engines/` | `scoring/parents/identity_background.scoring.json`. | Source/archive candidate; active runtime engines are under `lib/engines/`. |
| `styles/` | `styles.tsx`. | Archive/delete candidate after checking whether it duplicates `lib/styles.ts`. |
| `utils/` | `loadContent.js`, `supabase.ts`. | Archive/delete candidate after checking no legacy script relies on them. |
| `packages/` | `shared-types/src` type files. | Archive candidate; no package workspace reference found in `package.json`, but types may be useful source. |
| `cli/` | Older PowerShell CLI scripts. | Archive candidate; compare against `SafeStepsTools/` before deleting. |
| `automation/` | `create_casefile.ps1` and parent identity metadata. | Keep/migrate; not a deletion candidate yet. |
| `PowerShell/` | Local module copies and logs. | Archive/source candidate; mine selectively if needed. |

## Local Tool And Agent State - Do Not Commit

These are not app product code and should normally be ignored or kept out of release commits:

`.app-store/`, `.azure/`, `.claude/`, `.codemaker/`, `.codex/`, `.commandcode/`, `.config/`, `.copilot/`, `.cursor/`, `.docker/`, `.expo/`, `.gemini/`, `.iflow/`, `.redhat/`, `.sbx-denybin/`, `.vscode-shared/`, `.worktrees/`, `WPS Cloud Files/`

Some may contain local credentials, tokens, caches, generated state, or other machine-specific material. Do not publish or bulk-stage these.

## Current File Counts

The largest non-runtime or mixed folders observed in this pass were:

| Path | File count |
| --- | ---: |
| `.worktrees/` | 97,206 |
| `.codex/` | 32,410 |
| `Challenges/` | 7,439 |
| `challenges_clean/` | 7,439 |
| `backend/` | 5,991 |
| `snapshots/` | 3,574 |
| `data/` | 1,637 |
| `CHILD PROTECTION MANUAL FRAMEWORK GUIDELINES/` | 382 |
| `supabase/` | 214 |
| `lib/` | 188 |
| `app/` | 158 |

## Git State Blocker

The cleanup work should not delete or commit files until the Git state is corrected.

Current observations:

| Check | Result |
| --- | --- |
| Worktree root | `C:/Users/SAFES/SafeStepsApp` |
| Current branch | `main` |
| `HEAD` | Unborn/invalid on the current branch; `git rev-parse HEAD` fails. |
| Local history | Other branches contain commits, including `agent/final-backend-integration`, `agent/production-readiness-closeout-2026-08-14`, `agents/auto-patch-safesteps-engines`, `codex/pr-66-review-fix`, and `codex/tier-2-family-support-ecosystem`. |
| Remote config | `origin` is still `https://github.com/<your-username>/<repo-name>.git`, which is a placeholder, not a real project remote. |
| Remote refs present | `origin/main` points to `e7fab225` locally, but the configured URL is not reliable. |
| Staged file count | 32,687 files are staged as additions. |

Largest staged top-level groups:

| Path | Staged files |
| --- | ---: |
| `curriculum/` | 11,140 |
| `challenges_clean/` | 7,439 |
| `Challenges/` | 7,439 |
| `snapshots/` | 3,574 |
| `data/` | 1,637 |
| `supabase/` | 204 |
| `docs/` | 197 |
| `lib/` | 188 |
| `app/` | 158 |
| `__tests__/` | 91 |
| `.copilot/` | 99 |
| `.config/` | 46 |
| `.iflow/` | 42 |
| `.claude/` | 41 |
| `.codemaker/` | 37 |
| `.commandcode/` | 36 |

Why this matters:

- The current `main` branch is not the real committed project line.
- The index includes local tool state and likely non-source material.
- A normal `git commit` from this state would publish too much.
- A destructive cleanup would be hard to review because there is no valid current `HEAD` on `main`.

Non-destructive recovery direction:

1. Choose the real base branch or worktree to continue from. The strongest local candidate is `origin/main`/`e7fab225`, but verify against the intended GitHub repo before trusting the placeholder remote.
2. Do not commit the current staged index as-is.
3. Do not run `git reset --hard` or delete folders until the intended base is confirmed.
4. Once a valid base is checked out in a clean worktree, apply cleanup in narrow batches using the classifications above.

## Safe Next Pass

1. Fix the git state first. The repo currently reports no commits on `main` and 32,687 staged additions, so deletion would be hard to review.
2. Run exact import checks for each candidate folder before moving or deleting it.
3. For source packs, move preserved source summaries into `docs/source-content/` or confirm the content already exists in typed `lib/data/*`.
4. Migrate `SafeStepsTools/New-SafeStepsCasefile.ps1` away from loose root `casefiles/` and `parents/` if those folders should be retired.
5. Only then delete generated/local folders in small batches, validating with `npx tsc --noEmit`, `npx expo lint`, and `npm run audit:routes`.
