export const checkpointRules = {
  knowledgeCheckpoint: {
    required: true,
    retryAllowed: true,
    purpose: "Learning check, not punishment"
  },
  scenarioCheckpoint: {
    required: true,
    retryAllowed: true,
    purpose: "Apply the lesson to a real-life family situation"
  },
  practicalActivity: {
    required: true,
    purpose: "Show the parent has practised the skill in real life"
  }
};
